# Keep line endings identical on every platform, so `prettier --check` cannot pass
# locally and fail in CI (or the reverse) purely over CRLF.
* text=auto eol=lf

# Generated by npm; keep diffs out of review.
package-lock.json linguist-generated=true -diff
